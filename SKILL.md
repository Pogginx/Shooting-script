---
name: pogginxbzz-shooting-script
description: 将剧本、小说、剧情梗概与场景参考图拆成专业可拍摄的分镜头脚本，并按 Seedance 2.5 或 Seedance 2.0 的能力生成镜头切割、生成批次、素材绑定和可复制提示词。用户提到分镜、分镜头脚本、shot list、storyboard、镜头拆解、机位运镜、短剧切片、Seedance/即梦视频、逐镜提示词、首尾帧衔接、场景连续性，或要求检查现有分镜的时长、轴线、动作、视线、道具、声音与参考图匹配时使用。
---

# 专业分镜与 Seedance 镜头切割

把叙事内容先转成平台无关的“母版分镜”，再按目标模型切成可稳定生成的批次。剧本约束剧情、对白和动作；场景图约束空间、美术与光线；Seedance 版本约束每批时长、镜头组织和素材引用方式。不要为了适配模型而静默改戏。

## 先确定交付层级

从用户已给内容中推断，只有缺失信息会实质改变结果时才问一个简短问题。

- 用户只要普通分镜：输出母版分镜，不强行加入 Seedance 语法。
- 用户提到 Seedance 但未指定版本：默认按 Seedance 2.5 规划，并明确这一假设。
- 用户指定 2.0 或旧工作流：使用 2.0 兼容规则，不套用 2.5 的精确时间戳能力。
- 用户要求 Word：完成内容后交付真正的 `.docx`，不要把 Markdown 文本冒充 Word 文件。
- 用户只给剧本、没有场景图：可以拆镜，但将未锁定的空间与美术列为“待生成资产/待确认”，不要阻塞整个任务。
- 用户要求严格继承上传场景图：必须先逐张读取场景图；关键反打或反向空间不可见且会改变核心调度时，再请求补图。

## 按需读取参考资料

- 涉及 Seedance 时，先读 [references/seedance-models.md](references/seedance-models.md)，选择 2.5 或 2.0 配置。
- 需要切割剧本、分配时长或组织生成批次时，读 [references/shot-cutting.md](references/shot-cutting.md)。
- 需要可直接复制的模型提示词或具体交付表时，读 [references/prompt-contracts.md](references/prompt-contracts.md)。
- 遇到多人反应、对白、动作特写、危机建立、跨场转场时，读 [references/shot-patterns.md](references/shot-patterns.md)。

只加载当前任务需要的资料，不把全部模板无差别塞进结果。

## 输入分析

### 剧本

完整读取后提取：场次、叙事目标、情绪转折、人物关系、可见动作、对白、道具、时间、声音、特效和场景转换。把“害怕、意识到、爱上”等不可直接拍摄的心理词改成眼神、呼吸、停顿、重心、手部动作或对白反应。

### 场景图

逐张提取并记录：

- 固定结构：入口、门窗、墙体、楼梯、走廊、层高和不可穿越区域；
- 调度条件：人物站位、行动路径、前中后景、遮挡、正反打与中性机位；
- 美术锚点：主色、材质、家具、关键陈设、磨损与时代信息；
- 光线锚点：主光方向、色温、实景光源、阴影和曝光气质；
- 未知区域：单张图无法证明的反向结构或空间关系。

不要把场景图里偶然出现的人物自动当成剧本角色，不要让摄影机穿墙、穿家具或凭空创造门窗和纵深。

### 素材绑定

为每个参考素材规定一个主要职责和作用范围，例如：

```text
@图片1 → Maya 的脸、发型与服装 → 全批次
@图片2 → 主帐空间、火盆位置与暖光方向 → G02–G04
@视频1 → 只参考奔跑动作和跟拍节奏 → G03；不继承原人物与背景
@音频1 → Maya 的英文音色 → 她的对白；不作为 BGM
```

多人镜头建立角色台账：外形锚点、服装、道具归属、画面左右、朝向、声线和当前情绪。素材编号必须与实际上传顺序一致。

## 核心工作流

1. **建立叙事节拍表**：按“新信息、动作变化、情绪转折、视角变化、场景变化”划分节拍。
2. **检查剧本—场景冲突**：若剧本所需门窗、通道、站位或动作空间在参考图中不存在，保留剧情意图，标明冲突并提出最小调整，不静默改图或改戏。
3. **建立连续性圣经**：锁定角色、场景地理、光线、道具手别、轴线、视线、声音状态和禁用项。
4. **生成母版分镜**：每镜只承担一个主要视觉/心理功能，使用全片连续时间码。
5. **划分生成批次**：母版镜头不等于模型生成批次。把能共享角色、场景、光线、动作因果和参考素材的相邻镜头编入同一批；具体规则见 `shot-cutting.md`。
6. **选择镜头组织方式**：2.5 可用整数秒时间戳或“镜头 N”；2.0 默认只用“镜头 N”排序，不强制精确秒点。
7. **生成逐批提示词**：先写素材职责，再写全局设定、镜头/节拍、声音与连续性保护；不要堆砌互相冲突的运镜。
8. **检查可执行性**：对白必须能自然说完，复杂动作必须遵守准备→发力→接触→反应→恢复的因果链。
9. **校验时间线**：镜头无重叠、无无意空档，总时长等于目标时长。需要确定性检查时运行 `scripts/validate_timeline.py`。

## 分镜设计原则

- 先按叙事功能切镜，再按模型能力分批；禁止每三秒机械切一镜。
- 建立镜头交代空间，中近景承载关系，近景承载心理，特写只突出关键动作或道具。
- 保持 180 度轴线；必须越轴时，先用中性机位或可理解的运动建立新轴线。
- 镜尾为下一镜提供动作、视线、声音、遮挡、形状或光线匹配点。
- 同场多镜保持人物左右关系、服装、发型、道具手别、家具布局、主光方向与材质。
- 多人反应错峰发生，并为每个人设计不同的身体或面部反应。
- 克制表演优先使用眼神、呼吸、停顿、指尖和重心变化。
- 危机按正常→轻微异常→升级→警戒/转场逐级建立，不提前泄露高潮。
- 只有在叙事需要时使用绕拍、快速旋转、希区柯克变焦或 FPV；不要把运镜术语当装饰。
- 参考视频必须说明“参考什么”和“不继承什么”，避免角色、背景、品牌或音轨污染。

## 默认交付结构

按用户要求删减，不输出空占位符。

### 1. 输入、假设与模型配置

列出剧本范围、场景图映射、目标版本、总时长、画幅、对白语言和必要假设。

### 2. 连续性圣经

列出角色、场景空间、光线、道具、轴线、声音、风格和禁止项。

### 3. 生成批次表

| 批次 | 母版时间 | 模型/模式 | 包含镜头 | 素材 | 起始状态 | 结束锚点 |
|---|---|---|---|---|---|---|

### 4. 母版分镜表

| 镜号 | 全片时间码 | 时长 | 景别/焦段 | 机位/运镜 | 画面与动作 | 表演/台词 | 声音 | 连续性 | 批次 |
|---|---|---:|---|---|---|---|---|---|---|

每个镜头必须含：本镜功能、可见动作、镜尾状态和下一镜剪辑点。焦段无法可靠确定时给出建议值，不把建议写成场景图已经证明的事实。

### 5. Seedance 逐批提示词

每批提供一段可直接复制的完整提示词。2.5 使用整数秒时间段时覆盖整个批次；2.0 使用镜头编号与自然节奏。具体格式见 `prompt-contracts.md`。

### 6. 校验结果

只报告有用结论：总时长、批次时长、对白可说性、轴线、视线、动作承接、道具归属、参考素材映射、场景可行性和待确认项。

## 质量门槛

输出前逐项确认：

- 剧情顺序、对白和人物动机未被静默改写；
- 母版镜头与生成批次分层清楚；
- 版本规则正确：2.5 不误用旧 15 秒上限，2.0 不伪装成精确时间戳控制；
- 每个参考素材都有明确职责、范围和必要的排除项；
- 每批只有模型在该时长内能完成的动作与镜头数量；
- 时间范围有序、无重叠，总时长正确；
- 角色身份、画面左右、道具手别、动作动量、光线和场景地理连续；
- 声音、对白、BGM、字幕和“不要生成”要求互不矛盾；
- 结果包含可直接使用的最终提示词，而不是让用户从分析中自行拼装。
